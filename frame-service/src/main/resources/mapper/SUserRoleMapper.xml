<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.tj123.frame.service.mapper.SUserRoleMapper">

  <select id="list" resultType="java.util.Map" >
    select
      id,
      user_id userId,
      role_id roleId
    from s_user_role
  </select>

  <select id="get" resultType="java.util.Map" >
    select
      id,
      user_id userId,
      role_id roleId
    from s_user_role where id = #{id}
  </select>
  
  <select id="getUserRole" resultType="java.lang.String">
    select role_id from s_user_role where user_id = #{id}
  </select>

</mapper>
